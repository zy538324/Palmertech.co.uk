# Palmertech.co.uk environment configuration
# Populate these values and supply to Cloud Run or local development via `python-dotenv`.

# Flask session secret (set to a long, random string).
SECRET_KEY=

# SendGrid configuration
SENDGRID_API_KEY=
MAIL_DEFAULT_SENDER=no-reply@palmertech.co.uk
MAIL_OWNER_RECIPIENT=contact@palmertech.co.uk
PALMERTECH_REQUIREMENTS_TEMPLATE_ID=
PALMERTECH_REQUIREMENTS_RECIPIENT=projects@palmertech.co.uk

# Optional runtime overrides
HOST=0.0.0.0
PORT=8080
